# =============================================================================
# YOLO-REST Configuration Template
# =============================================================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control!
#
# For docker-compose usage:
#   1. cp .env.example .env
#   2. Set ROBOFLOW_API_KEY with your actual key from roboflow.com
#   3. (Optional) Configure Google Cloud credentials for Speech-to-Text features
#   4. Run: docker-compose up -d
# =============================================================================

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================
# Port to bind the server (Cloud Run will inject this automatically)
# Docker-compose sets PORT=8080 internally, mapped to host port 8000
# PORT=8080

# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# =============================================================================
# VIDEO PROCESSING
# =============================================================================
# Frame sampling rate (frames per second to process)
VIDEO_FPS=3

# =============================================================================
# AUDIO PROCESSING
# =============================================================================
# Audio sample rate in Hz
AUDIO_SAMPLE_RATE=16000

# Audio frame duration in milliseconds
AUDIO_FRAME_MS=20

# Audio window duration for emotion detection (seconds)
AUDIO_WINDOW_SEC=1.0

# Audio overlap duration for STT rotation (milliseconds)
AUDIO_OVERLAP_MS=1000

# Voice Activity Detection aggressiveness (0-3, higher = more aggressive)
VAD_AGGRESSIVENESS=1

# =============================================================================
# GOOGLE SPEECH-TO-TEXT CONFIGURATION
# =============================================================================
# Language code for speech recognition (e.g., pt-BR, en-US, es-ES)
STT_LANGUAGE=pt-BR

# Audio sample rate for STT (must match AUDIO_SAMPLE_RATE)
STT_SAMPLE_RATE=16000

# Google STT model to use
STT_MODEL=latest_long

# Enable automatic punctuation in transcripts
STT_ENABLE_PUNCTUATION=true

# Use single utterance mode (false for continuous streaming)
STT_SINGLE_UTTERANCE=false

# Maximum duration per STT stream before rotation (seconds)
STT_MAX_DURATION_SEC=240

# =============================================================================
# EMOTION DETECTION MODEL
# =============================================================================
# HuggingFace model ID for emotion classification
EMOTION_MODEL_ID=prithivMLmods/Speech-Emotion-Classification

# =============================================================================
# ROBOFLOW API CONFIGURATION
# =============================================================================
# Roboflow API endpoint URL
ROBOFLOW_API_URL=https://serverless.roboflow.com

# Roboflow API key (REQUIRED - get from roboflow.com)
ROBOFLOW_API_KEY=your_roboflow_api_key_here

# Roboflow model ID to use for inference
ROBOFLOW_MODEL_ID=human-face-emotions/28

# =============================================================================
# EVENT FORWARDING / EXTERNAL API
# =============================================================================
# Base URL for event forwarding (e.g., your backend API)
EVENT_FORWARD_BASE_URL=http://localhost:8080

# API key for authenticating with event forwarding endpoint
API_KEY=your_api_key_here

# HTTP request timeout for event forwarding (seconds)
HTTP_REQUEST_TIMEOUT_SEC=10.0
# =============================================================================